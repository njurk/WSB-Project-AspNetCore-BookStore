@model BookStore.Data.Data.Entities.BookGenre

@{
    ViewData["Title"] = "Book Genre Details";
}

<h1 class="mb-4 text-center">Book Genre Details</h1>

<div class="d-flex justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="border p-4 rounded">

            <dl class="row mb-3">
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Book)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Book.Title)
                </dd>

                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Genre)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Genre.Name)
                </dd>
            </dl>

            <div class="text-center mt-4">
                <a asp-action="Edit" asp-route-id="@Model?.IdBookGenre" class="btn btn-primary w-100 mb-2">Edit</a>
                <a asp-action="Index" class="btn btn-secondary w-100">Back to List</a>
            </div>
        </div>
    </div>
</div>
