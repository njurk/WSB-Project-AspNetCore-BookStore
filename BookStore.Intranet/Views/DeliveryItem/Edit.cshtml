@model BookStore.Data.Data.Entities.DeliveryItem

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center mb-4">Edit Delivery Item</h1>

<div class="d-flex justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="border p-4 rounded">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="IdDeliveryItem" />

                <div class="form-group mb-3">
                    <label asp-for="IdDelivery" class="control-label">Delivery</label>
                    <select asp-for="IdDelivery" class="form-control" asp-items="ViewBag.IdDelivery"></select>
                    <span asp-validation-for="IdDelivery" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="IdBook" class="control-label">Book</label>
                    <select asp-for="IdBook" class="form-control" asp-items="ViewBag.IdBook"></select>
                    <span asp-validation-for="IdBook" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Quantity" class="control-label">Quantity</label>
                    <input asp-for="Quantity" class="form-control" />
                    <span asp-validation-for="Quantity" class="text-danger"></span>
                </div>

                <div class="form-group text-center">
                    <input type="submit" value="Save" class="btn btn-primary w-100 mb-2" />
                    <a asp-action="Index" class="btn btn-secondary w-100">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
