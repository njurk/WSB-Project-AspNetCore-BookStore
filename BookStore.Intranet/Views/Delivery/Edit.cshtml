@model BookStore.Data.Data.Entities.Delivery

@{
    ViewData["Title"] = "Edit";
}

<h1 class="text-center mb-4">Edit Delivery</h1>

<div class="d-flex justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="border p-4 rounded">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="IdDelivery" />

                <div class="form-group mb-3">
                    <label asp-for="IdSupplier" class="control-label">Supplier</label>
                    <select asp-for="IdSupplier" class="form-control" asp-items="ViewBag.IdSupplier"></select>
                    <span asp-validation-for="IdSupplier" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="DeliveryDate" class="control-label">Delivery Date</label>
                    <input asp-for="DeliveryDate" class="form-control" />
                    <span asp-validation-for="DeliveryDate" class="text-danger"></span>
                </div>

                <div class="form-group text-center">
                    <input type="submit" value="Save" class="btn btn-primary w-100 mb-2" />
                    <a asp-action="Index" class="btn btn-secondary w-100">Back to List</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
