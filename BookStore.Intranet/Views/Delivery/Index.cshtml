@model IEnumerable<BookStore.Data.Data.Entities.Delivery>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Supplier)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDelivery">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDelivery">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDelivery">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
