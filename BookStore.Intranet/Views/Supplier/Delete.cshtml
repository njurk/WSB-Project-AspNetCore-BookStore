@model BookStore.Data.Data.Entities.Supplier

@{
    ViewData["Title"] = "Delete";
}

<h1 class="mb-4 text-center">Delete Supplier</h1>

<div class="d-flex justify-content-center">
    <div class="col-md-8 col-lg-6">
        <h3 class="text-center mb-4">Are you sure?</h3>

        <div class="border p-4 rounded">

            <dl class="row mb-3">
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Name)
                </dd>

                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Email)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Email)
                </dd>

                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Phone)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Phone)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="IdSupplier" />
                <button type="submit" class="btn btn-danger w-100 mb-3">Delete</button>
            </form>

            <div class="text-center">
                <a asp-action="Index" class="btn btn-secondary w-100 text-decoration-none">← Back to List</a>
            </div>
        </div>
    </div>
</div>
